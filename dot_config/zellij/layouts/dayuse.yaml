---
template:
  direction: Horizontal
  parts:
    - direction: Vertical  # part 1
      borderless: true
      split_size:
        Fixed: 1
      run:
        plugin:
          location: "zellij:tab-bar"
    - direction: Vertical # part 2
      body: true
    - direction: Vertical # part 3
      borderless: true
      split_size:
        Fixed: 2
      run:
        plugin:
          location: "zellij:status-bar"

session:
  name: Dayuse
  attach: true

tabs:
  - name: "Dayuse"
    direction: Vertical
    parts:
      - direction: Horizontal
        parts:
          - direction: Horizontal
            run:
              command: {cmd: "zsh", args: ["-c", "cd /workspace/dayuse-com && pipenv run fab builder"]}
          - direction: Vertical
            run:
              command: {cmd: "zsh", args: ["-c", "cd /workspace/dayuse-com && pipenv shell"]}
      - direction: Horizontal
        split_size:
          Percent: 70
        run:
          command: {cmd: "zsh", args: ["-c", "cd /workspace/dayuse-com && pipenv run fab start:False && pipenv shell"]}
